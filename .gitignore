node_modules/
public/
.DS_Store
.env
.env.*

# .NET local build output
bin/
obj/
.nuget/

# Local captures you are still editing
samples/output/*.draft.txt
samples/output/images/

# local grounding env (endpoints/account names — never commit)
samples/.env.grounding
**/bin/
**/obj/

# Internal reconciliation notes (never publish)
DEMO-RECONCILIATION-NOTES.md

# The six hash-pinned runtime packages are committed; symbol packages are not.
local-feed/*.symbols.nupkg
